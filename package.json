{
  "name": "controle-de-entregas",
  "version": "1.1.0",
  "description": "Um aplicativo para entregadores autônomos rastrearem suas entregas e ganhos diários, quinzenais e mensais.",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/main.js --loader:.tsx=tsx --define:process.env.NODE_ENV='\"production\"' --minify"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.21.4",
    "typescript": "^5.4.5"
  }
}